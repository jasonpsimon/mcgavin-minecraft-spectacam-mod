plugins {
    id "dev.kikugie.stonecutter"
}

stonecutter.active "1.21.4" /* [SC] DO NOT EDIT */

// Stonecutter 0.7+ auto-registers a `chiseledBuild` task that builds every
// version — no explicit registration needed.